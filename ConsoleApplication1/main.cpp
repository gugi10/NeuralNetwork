#include <iostream>
#include "Net.h"
#include <vector>
#include <stdio.h>
#include <tchar.h>
using namespace std;
int main() {
	vector<int> Topology;
	Topology.push_back(2);
	Topology.push_back(5);
	Topology.push_back(1);
	return 0;
}